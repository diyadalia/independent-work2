import styled from '@xstyled/styled-components';
import { grid } from './constants';

// title of each column box
export default styled.h4`
  transition: background-color ease 0.2s;
  flex: 1;
  user-select: none;
  position: relative;
`;
